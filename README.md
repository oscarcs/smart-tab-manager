# smart-tab-manager

Initial local setup for the GitHub repository:

- Git repository initialized on `main`
- Remote `origin` set to `https://github.com/oscarcs/smart-tab-manager.git`
- Minimal starter files added

## Next steps

Define the project type and scaffold the app structure.
